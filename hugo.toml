baseURL = "/"
languageCode = "en-us"
title = "odyssey"
enableRobotsTXT = true

[params]
  # Shown in the header next to the site title
  tagline = "a running log of technical findings"
  author  = "Kent Ballon"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 4
    ordered = false

[[menu.main]]
  name = "All Findings"
  url  = "/"
  weight = 1
